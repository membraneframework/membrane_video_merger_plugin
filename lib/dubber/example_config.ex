%{
  fragments: [{stream_id_x, start, stop}, {stream_id_y, start, stop}, ..., {stream_id_z, start, stop}],
  stream_id_1: %{
    offset: number
  },
  stream_id_2: %{
    offset: number
  },

  ...

  stream_id_n: %{
    offset: number
  }
}
